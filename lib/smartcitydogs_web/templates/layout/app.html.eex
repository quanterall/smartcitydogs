<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>SmartCityDogs!</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">

    <div class="container-fluid">
	<div class="row "></div>
	<div class="container">
		<div class="row">
		<nav class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
        navbar navbar-expand-lg navbar-dark bg-transperent top-navbar w-100
      <% else %>
       navbar navbar-expand-lg navbar-dark green bg-transperent top-navbar w-100
     <% end %>">
  <a class="navbar-brand bg-green pl-3 pr-3" href="#">Logo</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
        navbar-nav mr-auto
      <% else %>
         navbar-nav mr-auto
      <% end %> ">
      <li class="nav-item ">
        <a class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
           nav-link
        <% else %>
         nav-link black
         <% end %>   " href="/">Начало</a>
      </li>
      <li class="nav-item">

        <a class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
           nav-link
        <% else %>
         nav-link black
      <% end %>" href="/fortheproject">За проекта</a>
      </li>
      <li class="nav-item">
        <a class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
        nav-link
      <% else %>
         nav-link black
      <% end %>"  <%= if @current_user do %> id="signals_all_nav"
                                         href="/signals"
                                         <% else %>
                                          href="#"
                                          id="signals_all_nav_no_user"
                                         <% end %>
                                         > Подадени сигнали</a>
      </li>
      <li class="nav-item">
        <a class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
        nav-link
      <% else %>
         nav-link black
      <% end %>" href="/news">Новини</a>
      </li>
      <li class="nav-item">
        <a class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
        nav-link
      <% else %>
         nav-link black
      <% end %>" href="/help">Помощ</a>
      </li>
      <li class="nav-item">
        <a class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
        nav-link
      <% else %>
         nav-link black
      <% end %>" href="/contact/new">Контакти</a>
      </li>
    </ul>

    <ul class="navbar-nav">

      <%= if @current_user do %>
      <li class="nav-item ">
        <a class="<%= if Path.join(["/" | @conn.path_info]) == "/" do %>
        nav-link user-menu
      <% else %>
         nav-link user-menu black
      <% end %>" href="#">
        	Здравей, <b><%= @current_user.first_name%></b> <i class="fa fa-user"></i>

        </a>
      </li>
      <li class="nav-item">
        <%= if Path.join(["/" | @conn.path_info]) == "/" do %>
          <%= link "Sign out", to: session_path(@conn, :delete, @current_user), method: "delete", class: "nav-link" %>
         <% else %>
          <%= link "Sign out", to: session_path(@conn, :delete, @current_user), method: "delete", class: "nav-link black" %>
      <% end %>
      </li>
      <% else %>
      <li class="nav-item ">
         <%= if Path.join(["/" | @conn.path_info]) == "/" do %>
          <a class="nav-link" id="nav-register" href="#" >Register</a>
          <!-- <%= #link "Register", to: user_path(@conn, :new), class: "nav-link" %> -->
        <% else %>
          <a class="nav-link black" id="nav-register" href="#" >Register</a>
         <!-- <%= #link "Register", to: user_path(@conn, :new), class: "nav-link black" %> -->
        <% end %>
      </li>
      <li class="nav-item ">
        <%= if Path.join(["/" | @conn.path_info]) == "/" do %>
          <a class="nav-link" id="nav-sign-in" href="#" >Sign in</a>
          <!-- <%=# link "Sign in", to: session_path(@conn, :new), class: "nav-link" %> -->
        <% else %>
          <a class="nav-link black" id="nav-sign-in" href="#" >Sign in</a>
          <!-- <%= #link "Sign in", to: session_path(@conn, :new), class: "nav-link black" %> -->
        <% end %>
      </li>
      <% end %>

      <li class="nav-item bg-white ">
        <a class="nav-link text-green new-signal-btn"  <%= if @current_user do %> 
                                         href="#"
                                         id="send-signal"
                                         <% else %>
                                          href="#"
                                          id="send-signal-no-user"
                                         <% end %> >ПОДАЙ СИГНАЛ</a>
      </li>
    </ul>
  </div>
</nav>
</div>
</div>

<div id="myModal-signal" class="modal-signal">

  <div class="modal-content-signal">
    <!-- <span class="close1">&times;</span>
    <div class="send-signal-container">
      testbokluk
    </div> -->
  </div>

</div>


      <main role="main">
      <div class="container mt-0">
<%= render @view_module, @view_template, assigns %>

      </main>

      <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
